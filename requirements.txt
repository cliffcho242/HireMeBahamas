# ============================================================================
# MASTERMIND FIX 2025 — ZERO ASYNCPG BUILD ERRORS FOREVER
# ============================================================================
# Strategy: Force binary wheels for asyncpg, NO source builds, NO compilation
# Install time: <8 seconds | Works on: Render Free, Vercel, Railway
# Zero requirements: gcc, libpq-dev, apt-get — ALL ELIMINATED
# ============================================================================

# Core Framework & Server
fastapi==0.115.0
uvicorn[standard]==0.31.0
gunicorn==22.0.0

# Database - NUCLEAR BINARY-ONLY INSTALLATION
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0

# Data Validation
pydantic==2.9.2

# Authentication & Security
python-jose[cryptography]==3.3.0
pyjwt==2.9.0

# File Uploads
python-multipart==0.0.9

# ============================================================================
# INSTALL COMMAND (COPY-PASTE):
# pip install --only-binary=:all: -r requirements.txt
# 
# The --only-binary flag MUST be in pip command, NOT in requirements.txt
# This ensures asyncpg installs from pre-built wheel ONLY
# ============================================================================
