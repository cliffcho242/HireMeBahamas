{
  "$schema": "https://railway.app/railway.schema.json",
  "_comment": "⚠️ DEPRECATED: Railway backend is no longer recommended. Use Render instead. See CORRECT_STACK.md for the official stack.",
  "_correctStack": {
    "frontend": "Vercel (CDN, Edge, static & dynamic UI)",
    "backend": "Render (Always-on Gunicorn service)",
    "database": "Neon PostgreSQL (managed, scalable)",
    "optional": "Redis (sessions, feeds, caching) - Phase 2"
  },
  "build": {
    "builder": "NIXPACKS",
    "dockerfilePath": null,
    "dockerfilePathComment": "Set to null to force Nixpacks builder and prevent Dockerfile usage. Railway will use nixpacks.toml configuration instead.",
    "dockerCompose": false
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 180,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1
  }
}
