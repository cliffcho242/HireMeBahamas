# ============================================================================
# IMMORTAL VERCEL DEPLOY 2025 — FastAPI + Postgres
# ============================================================================
# Zero 500/404/401 Forever | Python 3.12 | Vercel Serverless Compatible
# Install: pip install --upgrade pip && pip install --only-binary=:all: -r requirements.txt
# ============================================================================

# Core Framework & Server
fastapi==0.115.6
uvicorn[standard]==0.32.0
gunicorn==23.0.0

# Vercel Serverless Adapter (CRITICAL for Vercel deployment)
mangum==0.18.1

# Database - Binary-only installation (NO COMPILATION)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10

# Data Validation
pydantic==2.10.2
pydantic-settings==2.6.1

# Authentication & Security (with ALL dependencies)
# Updated to 3.5.0 to fix pyasn1 dependency conflict (allows pyasn1>=0.5.0)
python-jose[cryptography]==3.5.0
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.18

# HTTP Client & Utilities
httpx==0.28.1
python-dotenv==1.0.1
python-decouple==3.8

# Async utilities (for thread pool execution)
anyio==4.6.0

# Performance & Caching
redis==5.2.1
hiredis==3.1.0

# Real-time (optional - can be removed if not using Socket.IO)
python-socketio==5.15.0

# Monitoring (optional)
prometheus-client==0.21.1

# ============================================================================
# CRITICAL NOTES:
# 1. mangum adapter is required for Vercel serverless deployment
# 2. anyio is required for async password hashing in thread pool
# 3. python-decouple is used for config management
# 4. All database drivers are binary-only (no compilation needed)
# ============================================================================
