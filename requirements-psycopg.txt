# ============================================================================
# NUCLEAR ALTERNATIVE — ZERO WHEELS, ZERO COMPILE (psycopg[binary])
# ============================================================================
# Use this if asyncpg STILL fails for any reason
# psycopg[binary] has pre-built wheels for EVERY platform
# 100% drop-in replacement for asyncpg with SQLAlchemy
# ============================================================================

# Core Framework & Server
fastapi==0.115.0
uvicorn[standard]==0.31.0
gunicorn==22.0.0

# Database - NUCLEAR OPTION: psycopg[binary] instead of asyncpg
sqlalchemy[asyncio]==2.0.35
psycopg[binary,pool]==3.1.18

# Data Validation
pydantic==2.9.2

# Authentication & Security
# Updated to 3.5.0 to fix pyasn1 dependency conflict (allows pyasn1>=0.5.0)
python-jose[cryptography]==3.5.0
pyjwt==2.9.0

# File Uploads
python-multipart==0.0.9

# ============================================================================
# INSTALLATION:
# pip install --upgrade pip && pip install -r requirements-psycopg.txt
# 
# DATABASE_URL CHANGE REQUIRED:
# OLD: postgresql+asyncpg://user:pass@host:5432/db
# NEW: postgresql+psycopg://user:pass@host:5432/db
# 
# NO CODE CHANGES NEEDED - psycopg[binary] is SQLAlchemy-compatible
# ============================================================================
