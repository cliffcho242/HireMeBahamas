# ============================================================================
# VERCEL SERVERLESS — FastAPI + Postgres (2025)
# ============================================================================
# Install: pip install --upgrade pip && pip install --only-binary=:all: -r requirements.txt
# Zero 500s, zero compilation, instant cold starts
# ============================================================================

# Core Framework
fastapi==0.115.6
pydantic==2.10.2
pydantic-settings==2.6.1
uvicorn[standard]==0.31.0

# Database - Binary-only (NO COMPILATION EVER)
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.44
psycopg2-binary==2.9.11
aiosqlite==0.21.0

# Serverless Handler (Vercel)
mangum==0.19.0

# Authentication & Security (with all crypto dependencies)
python-jose[cryptography]==3.3.0
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
cryptography==43.0.3

# Performance & Caching
redis==7.1.0
orjson==3.11.4
aiocache[redis]==0.12.3

# Utilities
python-multipart==0.0.18
python-dotenv==1.0.1
httpx==0.27.2
pillow==11.0.0
cloudinary==1.41.0

# Monitoring
prometheus-client==0.21.0

# GraphQL (optional)
strawberry-graphql[fastapi]==0.244.0

# WebSocket support
python-socketio==5.11.4

# ============================================================================
# CRITICAL: Vercel uses --only-binary=:all: automatically
# No gcc, no wheel compilation, zero cold-start failures
# ============================================================================
