# ============================================================================
# VERCEL SERVERLESS API REQUIREMENTS — FastAPI + Postgres (2025)
# ============================================================================
# Install: pip install --upgrade pip && pip install --only-binary=:all: -r requirements.txt
# Zero compilation, zero build errors, production-ready
# ============================================================================

# Core Framework (Lightweight for serverless)
fastapi==0.115.5
pydantic==2.10.2

# Database - Binary-only (NO COMPILATION)
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.36

# Serverless Handler (Vercel)
mangum==0.19.0

# Authentication & Security
python-jose[cryptography]==3.3.0
pyjwt[crypto]==2.10.1
passlib[bcrypt]==1.7.4

# Utilities
python-multipart==0.0.18
python-dotenv==1.0.1

# Serverless Handler (Vercel-specific)
mangum==0.19.0

# ============================================================================
# CRITICAL: Use --only-binary=:all: flag to prevent asyncpg compilation
# Vercel will automatically use this in build if set in vercel.json
# ============================================================================
