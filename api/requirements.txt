# ============================================================================
# MASTERMIND FIX 2025 — VERCEL API REQUIREMENTS (ZERO BUILD ERRORS)
# ============================================================================

fastapi==0.115.0
uvicorn[standard]==0.31.0
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
pydantic==2.9.2
python-jose[cryptography]==3.3.0
pyjwt==2.9.0
python-multipart==0.0.9

# ============================================================================
# Vercel deployment: pip install --only-binary=:all: -r requirements.txt
# The --only-binary flag forces binary wheels, preventing compilation
# ============================================================================
