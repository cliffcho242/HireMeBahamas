# Root npm configuration
# This handles installs at the repository root level

# Use legacy peer deps to handle conflicts across multiple apps
# Repository contains both React 18 (frontend) and React 19 (next-app)
legacy-peer-deps=true

# Suppress noise during installs
loglevel=error
audit=false
fund=false
