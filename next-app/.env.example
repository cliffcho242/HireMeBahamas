# =============================================================================
# 🔒 ENVIRONMENT VARIABLES - FOREVER FIX (LOCKED)
# =============================================================================
# 
# ⚠️ CRITICAL: ENV VARIABLE LAW FOR VERCEL DEPLOYMENT
#
# This is a Next.js project (uses NEXT_PUBLIC_ prefix for frontend variables)
#
# ✅ CORRECT PREFIX FOR FRONTEND: NEXT_PUBLIC_
#    - Use: NEXT_PUBLIC_API_URL=https://your-backend.com
#    - Vercel WILL expose variables with NEXT_PUBLIC_ prefix to the client
#
# ❌ NEVER USE THESE (Vercel will NOT expose them):
#    - API_URL=...                    ❌ Missing prefix - won't work!
#    - VITE_API_URL=...               ❌ Wrong framework (Vite only)
#    - Unprefixed variables are backend-only (not available in browser)
#
# 🔐 BACKEND-ONLY VARIABLES (NO PREFIX):
#    - DATABASE_URL, JWT_SECRET, etc.
#    - These should NEVER have NEXT_PUBLIC_ or VITE_ prefix
#    - They are only available on the server side
#
# =============================================================================

# Environment Variables
# Copy this to .env.local and fill in your values

# Database (Vercel Postgres - auto-injected when connected)
# 🔒 Backend-only variables (no prefix needed)
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NO_SSL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# KV Store (Vercel KV - auto-injected when connected)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Authentication
JWT_SECRET=your-super-secret-key-minimum-32-characters-long

# Push Notifications (optional)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=admin@hiremebahamas.com

# Analytics (auto-injected by Vercel)
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=
