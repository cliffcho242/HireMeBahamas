# npm configuration for Next.js app with React 19
# Suppress deprecation warnings during build
loglevel=error
audit=false
fund=false

# Use legacy peer deps to handle React 19 peer dependency conflicts
# Required for packages that haven't updated peer deps to include React 19
legacy-peer-deps=true
