# npm configuration for Vercel deployments
# Suppress deprecation warnings during build
loglevel=error
audit=false
fund=false

# Use legacy peer deps to handle React version conflicts
# Allows lucide-react to work with both React 18 and React 19
legacy-peer-deps=true
