# ============================================================================
# Railway Ignore File - Backend Deployment Configuration
# ============================================================================
# Only deploy Python backend files to Railway
# Frontend and admin panel are deployed separately
#
# CRITICAL: Docker files are excluded because:
# - Railway uses Nixpacks build system (not Docker)
# - docker-compose.yml contains PostgreSQL server definition
# - Railway provides managed PostgreSQL as a separate service
# ============================================================================

# Frontend and admin panel (deployed separately)
admin_panel/
frontend/

# Docker files (Railway uses Nixpacks)
docker-compose.yml
docker/
Dockerfile
backend/Dockerfile
frontend/Dockerfile
frontend/Dockerfile.dev
.dockerignore

# Node.js
node_modules/

# Git and IDE
.git/
.vscode/

# Python cache
__pycache__/
*.pyc

# Logs
*.log

# Environment files (use Railway dashboard for environment variables)
.env.local
.env.example

# Test files
test_*
*_test.py
