# =============================================================================
# ⚠️  DEPRECATED: HireMeBahamas Backend - Render Deployment Configuration
# =============================================================================
# 
# ⛔ THIS FILE IS DEPRECATED AND SHOULD NOT BE USED ⛔
# 
# ⚠️  MIGRATION TO VERCEL + RAILWAY COMPLETE
# 
# 🚀 MIGRATE NOW: See MIGRATE_FROM_RENDER.md for complete migration guide!
# 
# New architecture:
# - Frontend: Vercel (Edge + Static)
# - Backend: Railway (Python/Flask) OR Vercel Serverless
# - Database: Railway PostgreSQL OR Vercel Postgres
# - Cost: $0-7/month (vs $25-50/month on Render)
# - Performance: <200ms response (vs 2-5 min cold starts)
# - Zero downtime: No 502 errors!
#
# ⛔ DO NOT DEPLOY TO RENDER ⛔
# This file is kept for historical reference only.
# All Render services should be deleted after migration.
# 
# If you are seeing errors about DATABASE_URL containing placeholder hostname 'host',
# it means you are using example values instead of real database credentials.
# 
# ✅ MIGRATION STEPS:
# 1. 📖 Read: MIGRATE_FROM_RENDER.md (complete step-by-step guide)
# 2. 💾 Backup: python3 scripts/backup_database.py --source render
# 3. 🚀 Deploy: Follow RENDER_TO_VERCEL_MIGRATION.md or RENDER_TO_RAILWAY_MIGRATION.md
# 4. ⚙️  Update: Configure VITE_API_URL in Vercel or remove it for serverless
# 5. ✅ Verify: python3 scripts/test_deployment.py --url https://your-app.vercel.app
# 6. 🗑️  Delete: Remove Render services after 1-2 weeks verification
#
# For quick reference: RENDER_MIGRATION_QUICKSTART.md
# For visual guide: RENDER_MIGRATION_VISUAL.md
#
# OLD CONFIGURATION (for reference only - DO NOT USE):
# - Standard plan ($25/mo) with 1GB RAM
# - Single worker + 180s timeout
# - Health check at /health with 300s grace period
# - Keep-alive background worker pinging every 45s
# =============================================================================

services:
  - type: web
    name: hiremebahamas-backend
    runtime: python
    region: oregon
    
    # ==========================================================================
    # PLAN SELECTION - Standard recommended for zero 502s
    # ==========================================================================
    # STARTER ($7/mo, 512MB RAM):
    #   - Good for low traffic, may have occasional slow cold starts
    #   - Set WEB_CONCURRENCY=1, WEB_THREADS=2
    #
    # STANDARD ($25/mo, 1GB RAM) - RECOMMENDED:
    #   - Always On, no 502s, fast responses
    #   - Set WEB_CONCURRENCY=1, WEB_THREADS=4
    # ==========================================================================
    plan: standard
    
    # Auto-scaling (Standard plan and above)
    scaling:
      minInstances: 1
      maxInstances: 3
      targetCPUPercent: 80
      targetMemoryPercent: 85
    
    # ============================================================================
    # MASTERMIND FIX 2025 — RENDER BUILD COMMAND (COPY-PASTE EXACTLY)
    # ============================================================================
    # Use build script to avoid Poetry auto-detection issues with pyproject.toml
    buildCommand: bash build.sh
    
    # ==========================================================================
    # START COMMAND - Uvicorn
    # ==========================================================================
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    
    envVars:
      - key: FLASK_ENV
        value: production
      - key: ENVIRONMENT
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: FRONTEND_URL
        value: https://hiremebahamas.vercel.app
      - key: PYTHONUNBUFFERED
        value: "true"
      - key: PYTHON_VERSION
        value: "3.12.0"
      # Nuclear configuration - single worker, high timeout
      - key: WEB_CONCURRENCY
        value: "1"
      - key: WEB_THREADS
        value: "4"
      - key: GUNICORN_TIMEOUT
        value: "180"
      - key: GUNICORN_KEEPALIVE
        value: "5"
      - key: PRELOAD_APP
        value: "true"
      # Database keepalive thread
      - key: DB_KEEPALIVE_ENABLED
        value: "true"
      # Trust Render's load balancer
      - key: FORWARDED_ALLOW_IPS
        value: "*"
      # Nuclear pool configuration
      - key: DB_POOL_SIZE
        value: "2"
      - key: DB_CONNECT_TIMEOUT
        value: "45"
      - key: DB_POOL_RECYCLE
        value: "120"
      # DATABASE_URL must be set manually in Render dashboard
      # ⚠️  DO NOT USE 'host' AS HOSTNAME - Use your actual database hostname!
      # ⚠️  Get the real connection string from your database provider
      # NUCLEAR FORMAT (URL-encoded spaces and equals):
      # postgresql://user:pass@YOUR-DB-HOSTNAME.example.com:5432/db?sslmode=require&options=-c%20jit%3Doff&connect_timeout=45
      # Alternative (non-encoded):
      # postgresql://user:pass@YOUR-DB-HOSTNAME.example.com:5432/db?sslmode=require&options=-c jit=off&connect_timeout=45
    
    # ==========================================================================
    # HEALTH CHECK - Nuclear Settings (2025 Render Edition)
    # ==========================================================================
    # AVAILABLE ENDPOINTS (all respond in <5ms):
    #   /health  - Instant liveness check, no DB (use for Render health path)
    #   /live    - Kubernetes-style liveness probe, no DB
    #   /ready   - Readiness check with DB connectivity (returns 503 if DB down)
    #   /ready/db - Full DB health with session (use for deep checks)
    #   /ping    - Simple "pong" response for keepalive workers
    #   /metrics - Prometheus metrics (for monitoring)
    #
    # RENDER DASHBOARD SETTINGS (COPY-PASTE):
    #   Health Check Path: /health
    #   Grace Period: 300 seconds (5 min - handles worst-case cold start)
    #   Health Check Timeout: 30 seconds
    #   Health Check Interval: 30 seconds (default)
    #
    # WHY /health AND NOT /ready?
    #   /health responds instantly (<5ms) even during cold starts
    #   /ready checks DB connectivity, which may timeout during cold starts
    #   Using /health prevents Render from killing the container prematurely
    # ==========================================================================
    healthCheckPath: /health

  # ==========================================================================
  # KEEP-ALIVE BACKGROUND WORKER
  # ==========================================================================
  # Pings /health every 45 seconds to prevent cold starts
  # Cost: $0 on Render Free tier for Background Workers
  # ==========================================================================
  - type: worker
    name: keep-alive
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install requests
    startCommand: python keep_alive.py
    envVars:
      - key: PYTHONUNBUFFERED
        value: "true"
      - key: RENDER_EXTERNAL_URL
        value: https://hiremebahamas.onrender.com

  # ==========================================================================
  # CRON JOB - Backup Keepalive
  # ==========================================================================
  # Pings /health every 5 minutes as backup
  # Uses minimal Docker image (5MB vs full Python)
  # ==========================================================================
  - type: cron
    name: keepalive-ping
    region: oregon
    plan: free
    schedule: "*/5 * * * *"
    dockerImage: curlimages/curl:latest
    dockerCommand: curl -f -s -o /dev/null https://hiremebahamas.onrender.com/health

  # ==========================================================================
  # CACHE WARMING CRON JOB
  # ==========================================================================
  # Pre-warms cache every 5 minutes for sub-100ms responses
  # ==========================================================================
  - type: cron
    name: cache-warmer
    region: oregon
    plan: free
    schedule: "*/5 * * * *"
    dockerImage: curlimages/curl:latest
    dockerCommand: curl -f -s -X POST https://hiremebahamas.onrender.com/warm-cache
