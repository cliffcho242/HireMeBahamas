# ============================================================================
# JWT AUTH BULLETPROOF — VERCEL-READY 2025
# ============================================================================
# Exact versions for python-jose[cryptography] + passlib[bcrypt]
# Works on Vercel Serverless
# Zero compilation required
# ============================================================================

# Core Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0

# Serverless Handler (Vercel/AWS Lambda)
mangum==0.19.0

# JWT Authentication — python-jose[cryptography]
python-jose[cryptography]==3.3.0
cryptography==43.0.3
ecdsa==0.19.0
pyasn1==0.6.1
rsa==4.9

# Password Hashing — passlib[bcrypt]
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# Database
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.44
psycopg2-binary==2.9.11

# Utilities
python-multipart==0.0.20
python-dotenv==1.2.1
python-decouple==3.8
email-validator==2.3.0
anyio==4.7.0

# HTTP Client
httpx==0.28.1

# File Upload
aiofiles==25.1.0

# ============================================================================
# DEPLOYMENT NOTES:
# 1. All packages have binary wheels for Python 3.12
# 2. No compilation required (no gcc/build-essential needed)
# 3. python-jose[cryptography] provides JWT signing/verification
# 4. passlib[bcrypt] provides password hashing
# 5. asyncpg for PostgreSQL async driver
# 6. Works on Vercel Serverless out of the box
# ============================================================================
