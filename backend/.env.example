# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL Database Connection
# Priority order: DATABASE_PRIVATE_URL > POSTGRES_URL > DATABASE_URL
#
# For Vercel Postgres (Recommended - Best Performance):
# POSTGRES_URL is automatically provided by Vercel when you create a Postgres database
# Copy from: Vercel Dashboard → Storage → Your Database → .env.local tab
POSTGRES_URL=postgresql://default:PASSWORD@ep-xxxxx.us-east-1.aws.neon.tech:5432/verceldb?sslmode=require
DATABASE_URL=postgresql://default:PASSWORD@ep-xxxxx.us-east-1.aws.neon.tech:5432/verceldb?sslmode=require
#
# For Railway Postgres (Alternative):
# DATABASE_PRIVATE_URL is automatically provided by Railway (uses internal network, no egress fees)
# DATABASE_URL is the public connection (has egress fees)
DATABASE_PRIVATE_URL=postgresql+asyncpg://hiremebahamas_user:hiremebahamas_password@localhost:5432/hiremebahamas
#
# For local development with Docker:
# DATABASE_URL=postgresql+asyncpg://hiremebahamas_user:hiremebahamas_password@localhost:5432/hiremebahamas

# Database Performance Tuning (Optional - already optimized for Vercel Postgres)
DB_POOL_SIZE=2              # Connection pool size (2 recommended for serverless)
DB_MAX_OVERFLOW=3           # Burst capacity connections
DB_POOL_RECYCLE=120         # Recycle connections every 2 minutes (prevents SSL EOF errors)
DB_SSL_MODE=require         # SSL mode: require, verify-ca, verify-full
DB_FORCE_TLS_1_3=true       # Force TLS 1.3 for Vercel Postgres/Neon (recommended)
DB_CONNECT_TIMEOUT=45       # Connection timeout in seconds (45s for cold starts)
DB_COMMAND_TIMEOUT=30       # Query timeout in seconds

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

SECRET_KEY=your-super-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
CLOUDINARY_NAME=your_cloudinary_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
REDIS_URL=redis://localhost:6379

# Full production mode
ENVIRONMENT=production
DB_ECHO=false
